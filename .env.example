# Import into Render, then fill MONGODB_URI from Atlas Connect > Drivers.
NODE_ENV=production
NODE_VERSION=24.11.0
HOST=0.0.0.0
APP_ORIGIN=https://asepsis-staffing.vercel.app
MONGODB_URI=
MONGODB_DB=asepsis
# Render supplies PORT automatically. No persistent disk is needed.
